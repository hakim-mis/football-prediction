@model List<LeaderboardUserViewModel>

@{
    ViewData["Title"] = "Leaderboard";

    string UserPhoto(string? path)
    {
        return string.IsNullOrWhiteSpace(path)
            ? Url.Content("~/img/default-avatar.svg")
            : Url.Content("~/" + path.TrimStart('/', '~'));
    }

    string BuildMobileInfo(string? designation, string? department)
    {
        var parts = new List<string>();

        if (!string.IsNullOrWhiteSpace(designation))
        {
            parts.Add(designation);
        }

        if (!string.IsNullOrWhiteSpace(department))
        {
            parts.Add(department);
        }

        return parts.Any() ? string.Join(" • ", parts) : "-";
    }
}

<style>
    .leaderboard-topbar {
        width: 100%;
    }

    .leaderboard-header-line {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 10px;
        width: 100%;
    }

    .leaderboard-title-pill {
        margin: 0;
        white-space: nowrap;
    }

    .leaderboard-back-btn {
        border-radius: 999px;
        font-weight: 700;
        white-space: nowrap;
    }

    .leaderboard-search-toolbar {
        display: flex !important;
        flex-direction: row !important;
        align-items: center !important;
        gap: 8px;
        width: 100%;
    }

    .leaderboard-search-box {
        position: relative;
        flex: 1 1 auto;
        min-width: 0;
    }

    .leaderboard-search-icon {
        position: absolute;
        left: 12px;
        top: 50%;
        transform: translateY(-50%);
        color: #64748b;
        z-index: 2;
    }

    .leaderboard-search-box .form-control {
        padding-left: 38px;
        min-height: 42px;
        border-radius: 12px;
        width: 100%;
    }

    .leaderboard-clear-btn {
        flex: 0 0 auto;
        width: auto !important;
        border: 1px solid #fecaca;
        color: #dc2626;
        background: #ffffff;
        font-weight: 700;
        white-space: nowrap;
        min-height: 42px;
        border-radius: 12px;
        padding-left: 14px;
        padding-right: 14px;
    }

        .leaderboard-clear-btn:hover,
        .leaderboard-clear-btn:focus {
            color: #ffffff;
            background: #dc2626;
            border-color: #dc2626;
        }

    @@media (max-width: 767.98px) {
        .leaderboard-topbar {
            margin-bottom: 12px !important;
        }

        .leaderboard-header-line {
            align-items: center;
        }

        .leaderboard-title-pill {
            font-size: .85rem;
            padding: 8px 12px;
        }

        .leaderboard-back-btn {
            padding: 6px 12px;
            font-size: .82rem;
        }

        .leaderboard-search-toolbar {
            display: flex !important;
            flex-direction: row !important;
            align-items: center !important;
            gap: 6px !important;
            width: 100%;
        }

        .leaderboard-search-box {
            flex: 1 1 auto;
            min-width: 0;
        }

            .leaderboard-search-box .form-control {
                min-height: 40px;
                font-size: .86rem;
                padding-left: 34px;
            }

        .leaderboard-search-icon {
            left: 10px;
        }

        .leaderboard-clear-btn {
            flex: 0 0 auto;
            width: auto !important;
            min-width: 74px;
            min-height: 40px;
            padding-left: 9px;
            padding-right: 9px;
            font-size: .8rem;
        }
    }
</style>

<div class="dashboard-card leaderboard-page p-3 p-md-4 mb-4">

    <div class="leaderboard-topbar mb-3">

        <div class="leaderboard-header-line">
            <span class="pill leaderboard-title-pill">
                <i class="bi bi-trophy"></i>
                Leaderboard Top 100
            </span>

            <button type="button"
                    class="btn btn-outline-primary btn-sm leaderboard-back-btn"
                    onclick="goBackOrDashboard()">
                <i class="bi bi-arrow-left"></i>
                Back
            </button>
        </div>

    </div>

    <div class="leaderboard-search-toolbar mb-3">
        <div class="leaderboard-search-box">
            <span class="leaderboard-search-icon">
                <i class="bi bi-search"></i>
            </span>

            <input type="search"
                   id="leaderboardSearch"
                   class="form-control"
                   placeholder="Search" />
        </div>

        <button type="button"
                class="btn leaderboard-clear-btn"
                id="leaderboardClearSearch">
            <i class="bi bi-x-circle"></i>
            Clear
        </button>
    </div>

    <div class="leaderboard-table-wrap">
        <table class="table table-clean table-hover align-middle mb-0 leaderboard-table">
            <thead>
                <tr>
                    <th>Rank</th>
                    <th>Player</th>
                    <th class="leaderboard-designation-col">Designation</th>
                    <th class="leaderboard-department-col">Department</th>
                    <th>Total Score</th>
                    <th>Exact</th>
                    <th>Detail</th>
                </tr>
            </thead>

            <tbody id="leaderboardTableBody">
                @if (Model == null || !Model.Any())
                {
                    <tr>
                        <td colspan="7" class="text-center text-muted py-4">
                            No active user found.
                        </td>
                    </tr>
                }
                else
                {
                    foreach (var user in Model)
                    {
                        var designation = string.IsNullOrWhiteSpace(user.Designation) ? "-" : user.Designation;
                        var department = string.IsNullOrWhiteSpace(user.Department) ? "-" : user.Department;
                        var searchableText = $"{user.RankText} {user.FullName} {designation} {department} {user.TotalScore} {user.ExactPredictionCount}".ToLowerInvariant();

                        <tr class="leaderboard-row-item"
                            data-search="@searchableText">
                            <td>
                                <span class="rank-circle">
                                    @user.RankText
                                </span>
                            </td>

                            <td>
                                <div class="leaderboard-player-cell">
                                    <img src="@UserPhoto(user.PhotoPath)"
                                         class="leaderboard-avatar"
                                         alt="@user.FullName" />

                                    <div class="leaderboard-player-info">
                                        <strong>@user.FullName</strong>

                                        <div class="small text-muted d-md-none leaderboard-mobile-subtitle">
                                            @BuildMobileInfo(user.Designation, user.Department)
                                        </div>
                                    </div>
                                </div>
                            </td>

                            <td class="leaderboard-designation-col">
                                @designation
                            </td>

                            <td class="leaderboard-department-col">
                                @department
                            </td>

                            <td>
                                <span class="score-pill">
                                    @user.TotalScore pts
                                </span>
                            </td>

                            <td>
                                <span class="badge @(user.ExactPredictionCount == 0 ? "text-bg-secondary" : "text-bg-primary")">
                                    @user.ExactPredictionCount
                                </span>
                            </td>

                            <td>
                                <a asp-controller="Dashboard"
                                   asp-action="UserPredictionDetail"
                                   asp-route-userId="@user.UserId"
                                   class="btn btn-outline-primary btn-sm">
                                    <i class="bi bi-list-check"></i>
                                    Detail
                                </a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>

    <div id="leaderboardNoResult" class="empty-state mt-3 d-none">
        No player matched your search.
    </div>

</div>

@section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            bindLeaderboardSearch();
        });

        function goBackOrDashboard() {
            if (window.history.length > 1) {
                window.history.back();
                return;
            }

            window.location.href = '@Url.Action("Index", "Dashboard", new { area = "" })';
        }

        function bindLeaderboardSearch() {
            const searchInput = document.getElementById("leaderboardSearch");
            const clearButton = document.getElementById("leaderboardClearSearch");

            if (searchInput) {
                searchInput.addEventListener("input", function () {
                    applyLeaderboardSearch();
                });
            }

            if (clearButton) {
                clearButton.addEventListener("click", function () {
                    if (searchInput) {
                        searchInput.value = "";
                        searchInput.focus();
                    }

                    applyLeaderboardSearch();
                });
            }
        }

        function applyLeaderboardSearch() {
            const searchInput = document.getElementById("leaderboardSearch");
            const noResult = document.getElementById("leaderboardNoResult");

            const searchText = searchInput
                ? searchInput.value.trim().toLowerCase()
                : "";

            let visibleCount = 0;

            document.querySelectorAll(".leaderboard-row-item").forEach(function (row) {
                const searchable = row.getAttribute("data-search") || "";
                const shouldShow = searchText === "" || searchable.includes(searchText);

                if (shouldShow) {
                    row.classList.remove("d-none");
                    visibleCount++;
                } else {
                    row.classList.add("d-none");
                }
            });

            if (noResult) {
                if (visibleCount === 0 && document.querySelectorAll(".leaderboard-row-item").length > 0) {
                    noResult.classList.remove("d-none");
                } else {
                    noResult.classList.add("d-none");
                }
            }
        }
    </script>
}